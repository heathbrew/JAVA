/*Write a class that implements the CharSequence interface found in the 
java.lang package. Your implementation should return the string backwards. 
Select one of the sentences from this book to use as the data. 
Write a small main method to test your class; make sure to call all four methods.*/

/*Exercise 2: Suppose that you have written a time server, which periodically 
notifies its clients of the current date and time. Write an interface that the 
server could use to enforce a particular protocol on its clients.*/
/*
import java.time.*;
interface time {
    public void Time(int hour, int minute, int second);
    public void Date(int day, int month, int year);
    public void DateAndTime(int day, int month, int year,int hour, int minute, int second);
  }

class timeserver implements time{

}
*/
public class interfaceques1  {
    
}